MEMORY {
    ZP:   file = "",  define = yes, start = $0080, size = $0080;
    ROM:  file = %O,  fill = yes,   start = $C000, size = $4000;
}
SEGMENTS {
    ZEROPAGE: load = ROM, type = zp;
    CODE:     load = ROM, type = ro;
    VECTORS:  load = ROM, type = ro, start = $FFFA;
}
